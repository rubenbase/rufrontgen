import React from "react";
import { View, Text } from "react-native";

export default ({ navigation }) => (
  <View style={{ paddingVertical: 20 }}>
    <Text style={{ color: "white", fontSize: 28 }}>Loading...</Text>
  </View>
);
