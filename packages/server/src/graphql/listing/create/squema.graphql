input CreateListingInput {
  name: String!
  category: String!
  description: String!
  price: Int!
  # pictureUrl: string;
  latitude: Float!
  longitude: Float!
  amenities: [String!]!
}

type Listing {
  id: ID!
}

type Mutation {
  createListing(input: CreateListingInput!): Boolean!
}
